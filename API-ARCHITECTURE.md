# API Архитектура проекта

## 📁 Структура

```
app/
  api/
    posts.ts              # API функции (работают везде)
  composables/
    usePosts.ts           # TanStack Query hooks
  pages/
    posts-clean.vue       # Пример использования

server/
  api/
    posts.get.ts          # Кэшированный endpoint (ПРИМЕР)
```

## 🎯 Два подхода на примере

### 1️⃣ С серверным кэшем (fetchPosts)

**Для чего:** Показать как работает серверное кэширование

```
Client → /api/posts → Server Cache (5 мин) → JSONPlaceholder
         ✅ Кэш для всех пользователей
```

**Файлы:**
- `app/api/posts.ts` → `fetchPostsCached()` - вызывает server endpoint
- `server/api/posts.get.ts` → `defineCachedEventHandler` - кэширует на 5 минут
- Использует: `app/composables/usePosts.ts` → `usePostsQuery()`

### 2️⃣ Без серверного кэша (остальные функции)

**Для чего:** Простые запросы не требуют серверного proxy

```
Client → JSONPlaceholder (напрямую)
         ✅ Проще, меньше кода
```

**Файлы:**
- `app/api/posts.ts` → `fetchPostById()`, `createPost()`, `updatePost()`, `deletePost()`
- Напрямую к внешнему API

## 🔄 Двухуровневое кэширование (fetchPosts)

```
┌─────────────────────────────────────────────┐
│  Client (TanStack Query)                    │
│  - staleTime: 5 сек                         │
│  - Кэш в браузере                           │
│  - Для каждого пользователя свой            │
└─────────────────────────────────────────────┘
              ↓ refetch (через 5 сек)
┌─────────────────────────────────────────────┐
│  Server (defineCachedEventHandler)          │
│  - maxAge: 5 минут                          │
│  - Кэш в Node.js                            │
│  - ОБЩИЙ для всех пользователей             │
└─────────────────────────────────────────────┘
              ↓ cache miss (через 5 мин)
┌─────────────────────────────────────────────┐
│  External API (JSONPlaceholder)             │
└─────────────────────────────────────────────┘
```

## 📝 Когда использовать server/api/

### ✅ Используй `server/api/` с кэшем когда:

```typescript
// Секретные API ключи
export default defineCachedEventHandler(async () => {
  return await $fetch('https://api.openai.com/v1/chat', {
    headers: { 'Authorization': `Bearer ${process.env.OPENAI_KEY}` }
  })
}, { maxAge: 60 })
```

```typescript
// Дорогие запросы к БД
export default defineCachedEventHandler(async () => {
  return await prisma.product.findMany({
    include: { category: true, reviews: true }
  })
}, { maxAge: 300 })
```

```typescript
// API с лимитами запросов
export default defineCachedEventHandler(async () => {
  return await $fetch('https://api.weather.com/forecast')
}, { maxAge: 1800 })
```

### ❌ НЕ используй для:

- Публичных API без ключей (как JSONPlaceholder)
- Приватных данных (зависят от пользователя)
- Mutations (POST/PUT/DELETE)
- Часто меняющихся данных

## 🚀 Пример использования в компоненте

```vue
<script setup lang="ts">
import { fetchPostsCached } from '@/api/posts'
import { usePostsQuery } from '@/composables/usePosts'

// SSR: загружаем через server endpoint с кэшем
const { data: ssrData } = await useAsyncData('posts', () => fetchPostsCached(5))

// Client: TanStack Query с начальными данными из SSR
const { data: posts } = usePostsQuery(5, ssrData.value ?? undefined)
</script>
```

## 💡 Итог

Этот проект показывает **ОБА подхода**:

1. **С серверным кэшем** - для демонстрации возможностей
2. **Без серверного кэша** - для простых случаев

В реальном проекте выбирай подход в зависимости от требований:
- Нужны секреты / БД / лимиты? → `server/api/` с кэшем
- Публичный API? → Прямые запросы проще